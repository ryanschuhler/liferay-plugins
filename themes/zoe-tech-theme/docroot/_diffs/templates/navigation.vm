<nav class="$nav_css_class" id="navigation">
	<h1>
		<span>#language ("navigation")</span>
	</h1>

	<ul>
		#foreach ($nav_item in $nav_items)
			#if ($nav_item.isSelected())
				<li aria-selected="true" class="selected">
			#else
				<li>
			#end
				<a href="$nav_item.getURL()" $nav_item.getTarget()><span>$nav_item.icon() $nav_item.getName()</span></a>

				#if ($nav_item.hasChildren())
					<ul class="child-menu">
						#foreach ($nav_child in $nav_item.getChildren())
							#if ($nav_child.isSelected())
								<li aria-selected="true" class="selected">
							#else
								<li>
							#end
								<a href="$nav_child.getURL()" $nav_child.getTarget()>$nav_child.getName()</a>
							</li>
						#end
					</ul>
				#end
			</li>
		#end

		<div id="mobile-buttons">
			#if ($is_signed_in)
				#set ($signInButton = "$sign_out_url")
				#set ($signInText = "Sign Out")
				<div id="myAccountButton"><a href="$my_account_url"><span><img src="/html/icons/my_account.png"/>My Account</span></a></div>
			#else
				#set ($signInButton = "$sign_in_url")
				#set ($signInText = "Sign In")
			#end

			<div id="mobile-sign-out">
				<a href="$signInButton">
					<span><img src="${images_folder}/common/user_icon.png"/>$signInText</span>
				</a>
			</div>

			<div class="desktop-link">
				<a href="">
					<span><img src="${images_folder}/common/desktop.png"/>Go To Desktop View</span>
				</a>
			</div>
		</div>
	</ul>
</nav>